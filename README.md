# MediFlow

A full-stack Hospital Management System (v1).

## Structure

- `backend/` — API server (tech choice TBD: planned Node.js + Express or NestJS)
- `frontend/` — React (Vite) + Tailwind UI
- `database/` — schema, migrations, and seed scripts (PostgreSQL)
- `docs/` — requirements and design documents
- `tests/` — automated tests

## Docs

- `docs/requirements.md` — feature requirements (v1)
- `docs/database-design.md` — entities, constraints, and roles

## Status

Initial project scaffolding only. No implementation yet.